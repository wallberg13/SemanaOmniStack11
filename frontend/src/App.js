import React from "react";

import "./global.css";
import Routes from "./routes";

// JSX Javascrip e XML
/**
 * Um componente no JS é uma função que retorna HTML.
 */

/**
 * Estado: Uma informação que será mantida pelo o componente.
 * Não se altera o valor do estado da forma que a gente quiser, em
 * React, um estado é sobreposto.
 */

function App() {
  return <Routes />;
}

export default App;
